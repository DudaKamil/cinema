@model Cinema.Models.LoginModel

@{
    ViewBag.Title = "Zaloguj się";
}


<!-- All the files that are required -->
<link rel="stylesheet" href="//maxcdn.bootstrapcdn.com/font-awesome/4.3.0/css/font-awesome.min.css">
<link href="http://fonts.googleapis.com/css?family=Varela+Round" rel="stylesheet" type="text/css">
<script src="https://cdnjs.cloudflare.com/ajax/libs/jquery-validate/1.13.1/jquery.validate.min.js"></script>
<meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1"/>

<link href="~/Content/account/auth.css" type="text/css" rel="stylesheet"/>
<script src="~/Scripts/account/auth.js"></script>


@using (Html.BeginForm())
{
    <!-- LOGIN FORM -->
    <div class="text-center" style="padding: 50px 0">
        <div class="logo">Zaloguj się</div>
        <!-- Main Form -->
        <div class="login-form-1">
            <form id="login-form" class="text-left">
                @Html.AntiForgeryToken()
                <div class="login-form-main-message"></div>
                <div class="main-login-form">
                    <div class="login-group">
                        <div class="form-group">
                            @Html.LabelFor(m => m.Login, new {@class = "sr-only"})
                            @Html.TextBoxFor(m => m.Login, new {@class = "form-control", placeholder = "login"})
                        </div>

                        <div class="form-group">
                            @Html.LabelFor(m => m.Password, new {@class = "sr-only"})
                            @Html.TextBoxFor(m => m.Password, new {@class = "form-control", placeholder = "hasło"})
                        </div>

                        @*                         TODO: zdecydować czy funkcja "zapamiętaj mnie" potrzebna*@
                        @*                        <div class="form-group login-group-checkbox">*@
                        @*                            @Html.CheckBoxFor(m => m.RememberMe)*@
                        @*                            @Html.LabelFor(m => m.RememberMe)*@
                        @*                            <input type="checkbox" id="lg_remember" name="lg_remember">*@
                        @*                            <label for="lg_remember">zapamiętaj mnie</label>*@
                        @*                        </div>*@

                        <div id="validation-summary">
                            @Html.ValidationSummary()
                        </div>
                    </div>
                    <button type="submit" class="login-button">
                        <i class="fa fa-chevron-right"></i>
                    </button>
                </div>

                <div class="etc-login-form">
                    <p>
                        zapomniałeś hasła? <a href="#">kliknij tutaj</a>
                    </p>
                    <p>
                        nowy użytkownik? <a href="/Account/Register">zarejestruj się</a>
                    </p>
                </div>
            </form>
        </div>
        <!-- end:Main Form -->
    </div>
}